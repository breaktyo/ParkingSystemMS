spring:
  application:
    name: core-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

grpc:
  sensor:
    host: localhost
    port: 50051

soap:
  city:
    url: http://localhost:8081/ws/city